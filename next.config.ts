import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [{ hostname: 'ac.goit.global' }],
  },
};

export default nextConfig;

// feat: React-TS, HW9, task1, adds prompts and test-cases
