import NotesClient from './Notes.client';

export default async function NotesPage() {
  return <NotesClient />;
}

// client - browser/frontend
// nextServer
// nextServer - http to beckend - client
